import { AppRouteLoading } from "@/components/app/app-route-loading";

export default function SortWorkflowLoading() {
  return (
    <AppRouteLoading
      title="Loading Sort"
      subtitle="Preparing this Sort workflow without changing Apple Music."
    />
  );
}
